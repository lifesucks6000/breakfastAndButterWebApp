<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypture - Ultimate Crypto Solution</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&display=swap');

        body, html {
            margin: 0;
            padding: 0;
            font-family: 'Inter', sans-serif;
            background-color: #000;
            color: #fff;
            line-height: 1.2;
            overflow-x: hidden;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            opacity: 0;
            animation: fadeIn 1s ease-out forwards;
        }

        .logo {
            font-weight: bold;
            font-size: 18px;
            background: linear-gradient(to right, #fff, #888);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            transition: transform 0.3s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .nav-buttons {
            display: flex;
            gap: 20px;
        }

        .nav-button {
            background: none;
            border: none;
            color: #fff;
            cursor: pointer;
            font-size: 14px;
            background: linear-gradient(to right, #fff, #888);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            transition: transform 0.3s ease, opacity 0.3s ease;
        }

        .nav-button:hover {
            transform: translateY(-2px);
            opacity: 0.8;
        }

        .hero {
            text-align: center;
            padding-bottom: 30px;
            opacity: 0;
            animation: fadeIn 1s ease-out 0.5s forwards;
        }

        .hero h1 {
            font-size: 100px;
            font-weight: 300;
            margin-bottom: 20px;
            line-height: 1;
            text-align: center;
        }

        .hero h1 span {
            display: block;
            opacity: 0;
            transform: translateY(20px);
        }

        .hero h1 .ultimate {
            margin-left: 20%;
            width: 50%;
            background: linear-gradient(to right, #fff, #555);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: slideUp 0.8s ease-out 1s forwards;
        }

        .hero h1 .crypto {
            margin-left: 32%;
            width: 50%;
            background: linear-gradient(to left, #fff, #555);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: slideUp 0.8s ease-out 1.2s forwards;
        }

        .hero h1 .solution {
            margin-left: 15%;
            width: 50%;
            background: linear-gradient(to right, #fff, #555);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: slideUp 0.8s ease-out 1.4s forwards;
        }

        .hero p {
            font-size: 12px;
            color: #888;
            max-width: 300px;
            margin-left: 45%;
            background: linear-gradient(to left, #fff, #555);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            opacity: 0;
            animation: fadeIn 1s ease-out 2s forwards;
        }

        .scroll-indicator {
            font-size: 24px;
            color: #1e1b1b;
            width: 50px;
            height: 50px;
            line-height: 50px;
            border-radius: 50%;
            margin-top: 40px;
            margin-left: 60%;
            background: linear-gradient(to right, #fff, #363434);
            opacity: 0;
            animation: fadeInUp 1s ease-out 2.5s forwards, bounce 2s infinite 3.5s;
        }

        .about {
            text-align: center;
            padding: 40px 0;
            opacity: 0;
            transform: translateY(20px);
            animation: fadeInUp 1s ease-out forwards;
        }

        .about h2 {
            font-size: 14px;
            color: #888;
            margin-bottom: 20px;
            text-transform: uppercase;
            background: linear-gradient(to right, #fff, #888);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            display: inline-block;
            padding: 5px 15px;
            border: 1px solid rgba(255, 255, 255, 0.3);
            border-radius: 20px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .about h2:hover {
            transform: scale(1.05);
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
        }

        .about p {
            font-size: 18px;
            max-width: 700px;
            margin: 0 auto 20px;
            background: linear-gradient(to right, #fff, #888);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            opacity: 0;
            transform: translateY(20px);
            animation: fadeInUp 1s ease-out 0.5s forwards;
        }

        .cta-buttons {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
            opacity: 0;
            animation: fadeIn 1s ease-out 1s forwards;
        }

        .cta-button {
            padding: 10px 20px;
            border-radius: 25px;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 12px;
            border: none;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .cta-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 10px rgba(255, 255, 255, 0.2);
        }

        .cta-primary {
            background: linear-gradient(to right, #fff, #888);
            color: #000;
        }

        .cta-secondary {
            background: linear-gradient(to right, #333, #666);
            color: #fff;
        }

        .features {
            padding: 40px 0;
            opacity: 0;
            animation: fadeIn 1s ease-out forwards;
        }

        .features h2 {
            font-size: 36px;
            margin-bottom: 40px;
            width: 30%;
            font-weight: 400;
            background: linear-gradient(to right, #fff, #888);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-align: left;
            opacity: 0;
            transform: translateX(-20px);
            animation: slideRight 1s ease-out forwards;
        }

        .feature-list {
            display: flex;
            justify-content: space-between;
        }

        .feature-item {
            flex-basis: 30%;
            border-left: 1px solid rgba(255, 255, 255, 0.1);
            padding-left: 20px;
            opacity: 0;
            transform: translateY(20px);
        }

        .feature-item:nth-child(1) { animation: fadeInUp 1s ease-out 0.2s forwards; }
        .feature-item:nth-child(2) { animation: fadeInUp 1s ease-out 0.4s forwards; }
        .feature-item:nth-child(3) { animation: fadeInUp 1s ease-out 0.6s forwards; }

        .feature-number {
            font-size: 18px;
            width: 30px;
            color: #888;
            margin-bottom: 100px;
            background: linear-gradient(to right, #fff, #3e3d3d);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            transition: transform 0.3s ease;
        }

        .feature-item:hover .feature-number {
            transform: scale(1.1);
        }

        .feature-description {
            font-size: 12px;
            color: #888;
            background: linear-gradient(to right, #fff, #888);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            transition: opacity 0.3s ease;
        }

        .feature-item:hover .feature-description {
            opacity: 0.8;
        }

        footer {
            padding: 20px 0;
            font-size: 10px;
            color: #888;
            opacity: 0;
            animation: fadeIn 1s ease-out forwards;
        }

        .footer-links {
            display: flex;
            justify-content: space-between;
        }

        .footer-column {
            display: flex;
            flex-direction: column;
        }

        .footer-link {
            color: #888;
            text-decoration: none;
            margin-bottom: 5px;
            background: linear-gradient(to right, #fff, #888);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            transition: transform 0.3s ease, opacity 0.3s ease;
        }

        .footer-link:hover {
            transform: translateX(3px);
            opacity: 0.8;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        @keyframes fadeInUp {
            from { 
                opacity: 0;
                transform: translateY(20px);
            }
            to { 
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideUp {
            from { 
                opacity: 0;
                transform: translateY(20px);
            }
            to { 
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideRight {
            from { 
                opacity: 0;
                transform: translateX(-20px);
            }
            to { 
                opacity: 1;
                transform: translateX(0);
            }
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-10px);
            }
            60% {
                transform: translateY(-5px);
            }
        }

        .parallax {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            top: 0;
            left: 0;
            z-index: -1;
        }

        .parallax span {
            position: absolute;
            display: block;
            width: 20px;
            height: 20px;
            background: rgba(255, 255, 255, 0.05);
            animation: move 25s infinite linear;
        }

        @keyframes move {
            from { transform: translateY(100vh) rotate(0deg); }
            to { transform: translateY(-100vh) rotate(360deg); }
        }
    </style>
</head>
<body>
    <div class="parallax" id="parallax"></div>
    <div class="container">
        <header>
            <div class="logo">CRYPTURE</div>
            <div class="nav-buttons">
                <button class="nav-button">ORDER NOW</button>
                <button class="nav-button">MENU</button>
            </div>
        </header>

        <section class="hero">
            <h1>
                <span class="ultimate">ULTIMATE</span>
                <span class="crypto">CRYPTO</span>
                <span class="solution">SOLUTION</span>
            </h1>
            <p>CRYPTURE EMPOWERS YOU<br>TO MANAGE YOUR DIGITAL<br>ASSETS EFFORTLESSLY</p>
            <div class="scroll-indicator">↓</div>
        </section>

        <section class="about">
            <h2>ABOUT</h2>
            <p>OUR MISSION IS TO EMPOWER INDIVIDUALS<br>AND BUSINESSES WITH SEAMLESS ACCESS TO<br>CRYPTOCURRENCIES, OFFERING RELIABLE<br>SOLUTIONS FOR MANAGING DIGITAL ASSETS.</p>
            <p>DISCOVER HOW CRYPTURE REVOLUTIONIZES<br>FINANCE WITH CUTTING-EDGE TECHNOLOGY<br>AND UNMATCHED CUSTOMER SUPPORT.</p>
            <div class="cta-buttons">
                <button class="cta-button cta-primary">ORDER NOW</button>
                <button class="cta-button cta-secondary">DISCOVER PRICING</button>
            </div>
        </section>

        <section class="features">
            <h2>ADVANCED KEY<br>PROTECTION</h2>
            <div class="feature-list">
                <div class="feature-item">
                    <div class="feature-number">01</div>
                    <p class="feature-description">The card's chip generates<br>a unique private key that<br>remains hidden.</p>
                </div>
                <div class="feature-item">
                    <div class="feature-number">02</div>
                    <p class="feature-description">Cards securely connect to<br>transfer your encrypted<br>private keys safely.</p>
                </div>
                <div class="feature-item">
                    <div class="feature-number">03</div>
                    <p class="feature-description">Keys are distributed<br>across three cards with no<br>other copies existing.</p>
                </div>
            </div>
        </section>

        <footer>
            <div class="footer-links">
                <div class="footer-column">
                    <a href="#" class="footer-link">Features</a>
                    <a href="#" class="footer-link">About</a>
                    <a href="#" class="footer-link">Security</a>
                    <a href="#" class="footer-link">Pricing</a>
                    <a href="#" class="footer-link">FAQ</a>
                    <a href="#" class="footer-link">Contact Us</a>
                </div>
                <div class="footer-column">
                    <a href="#" class="footer-link">App Store</a>
                    <a href="#" class="footer-link">Google Play</a>
                </div>
                <div class="footer-column">
                    <a href="#" class="footer-link">LinkedIn</a>
                    <a href="#" class="footer-link">Discord</a>
                    <a href="#" class="footer-link">X (Twitter)</a>
                </div>
            </div>
            <p>© 2024 CRYPTURE</p>
        </footer>
    </div>
</body>
</html>